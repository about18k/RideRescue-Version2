<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="road_rescue.Driver_UI.gasStation"
             Title="Nearest Gas Stations"
             Shell.TabBarIsVisible="False"
             xmlns:driver="clr-namespace:road_rescue.Driver_UI"
             xmlns:local="clr-namespace:road_rescue">

    <ContentPage.Resources>
        <local:RatingToStarsConverter x:Key="RatingToStars" />
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,*">
        <!-- Header with Search -->
        <VerticalStackLayout Padding="10" Spacing="10" BackgroundColor="White" Grid.Row="0">
            <Label Text="Nearest Gas Stations"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,10" />
            <Border Stroke="Gray" StrokeThickness="1" Padding="0"
                    StrokeShape="RoundRectangle 100">
                <SearchBar Placeholder="Find gas stations near you.." 
                           BackgroundColor="Transparent"
                           VerticalOptions="Center"
                           HeightRequest="40"/>
            </Border>
        </VerticalStackLayout>

        <!-- Category Chips -->
        <ScrollView Orientation="Horizontal" Grid.Row="1" Margin="0,10">
            <HorizontalStackLayout Spacing="6" Padding="5,0">
                <Button Text="🛠 Repair" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🛞 Vulcanize" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="⛽ Gas" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🚑 Hospital" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🚨 Rescue" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="👮 Police" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🔥 Fire" Clicked="OnChipClicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
            </HorizontalStackLayout>
        </ScrollView>

        <!-- Gas Stations List -->
        <ScrollView Grid.Row="2">
            <VerticalStackLayout Padding="10" Spacing="10">
                <CollectionView ItemsSource="{Binding Places}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10" Padding="15" StrokeThickness="1" Stroke="#E0E0E0" BackgroundColor="#ffffff">
                                <Grid RowDefinitions="Auto,*,Auto" ColumnDefinitions="Auto,*">

                                    <Image Grid.RowSpan="2"
       Source="gas_station.png"
       WidthRequest="60"
       HeightRequest="60"
       Aspect="AspectFill"
       VerticalOptions="Center"
       HorizontalOptions="Start"
       Margin="0,0,10,0">
                                        <Image.Clip>
                                            <EllipseGeometry Center="30,30" RadiusX="30" RadiusY="30"/>
                                        </Image.Clip>
                                    </Image>

                                    <Label Grid.Column="1"
       Text="{Binding name}"
       FontSize="18"
       VerticalOptions="Start"
       TextColor="Black"
       FontAttributes="Bold"
       LineBreakMode="WordWrap"
       Margin="0,0,0,5"/>

                                    <StackLayout Grid.Row="2" Grid.Column="0"
             Orientation="Vertical"
             VerticalOptions="End"
             HorizontalOptions="Start"
             Spacing="2">

                                        <Label Text="{Binding plus_code}"
           FontSize="14"
           TextColor="Gray"
           VerticalOptions="Center"
                                               MaximumWidthRequest="200"/>



                                    </StackLayout>

                                    <VerticalStackLayout Grid.Row="2" Grid.Column="1" HorizontalOptions="End" Spacing="10" Margin="0,15,0,0" WidthRequest="100">
                                        <Button Text="Locate" Command="{Binding Source={RelativeSource AncestorType={x:Type driver:gasStation}}, Path=OpenMapsCommand}"
                                                CommandParameter="{Binding maps_link}"

            BackgroundColor="#21499F" TextColor="White"
            CornerRadius="20" HeightRequest="35" WidthRequest="100" Padding="0"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>


            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
