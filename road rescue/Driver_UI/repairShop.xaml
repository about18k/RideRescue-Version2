<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="road_rescue.Driver_UI.repairShop"
             Shell.NavBarIsVisible="False"
             xmlns:local="clr-namespace:road_rescue.Driver_UI"
             >
    <ContentPage.Resources>
        <local:RatingToStarsConverter x:Key="RatingToStars" />
    </ContentPage.Resources>
    <Grid RowDefinitions="Auto,Auto,*">
        <!-- Fixed Header (Search Bar) -->
        <VerticalStackLayout Padding="10" Spacing="10" BackgroundColor="White" Grid.Row="0">
            <Label Text="Nearest Repair Shop"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,10" />
            <Border Stroke="Gray" StrokeThickness="1" Padding="0"
                    StrokeShape="RoundRectangle 100">
                <SearchBar Placeholder="Find services near you.." 
                           BackgroundColor="Transparent"
                           VerticalOptions="Center"
                           HeightRequest="40"/>
            </Border>
        </VerticalStackLayout>

        <!-- Horizontal Scrollable Filter Buttons (Positioned below the search bar) -->
        <ScrollView Orientation="Horizontal" Grid.Row="1" Margin="0,10">
            <HorizontalStackLayout Spacing="6" Padding="5,0">
                <Button Text="🛠 Repair"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🛞 Vulcanize" Clicked="Button_Clicked"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="⛽ Gas" 
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🚑 Hospital" 
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🚨 Rescue" 
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="👮 Police"
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
                <Button Text="🔥 Fire" 
                        FontSize="12" Padding="10,5"
                        BackgroundColor="#f2f2f2" TextColor="Black"
                        BorderColor="Gray" BorderWidth="1"
                        CornerRadius="15" />
            </HorizontalStackLayout>
        </ScrollView>

        <!-- Scrollable Shop List (below buttons) -->
        <ScrollView Grid.Row="2">
            <VerticalStackLayout Padding="10" Spacing="10">
                <CollectionView ItemsSource="{Binding Places}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10" Padding="15" StrokeThickness="1" Stroke="#E0E0E0" BackgroundColor="#ffffff">
                                <Grid RowDefinitions="Auto,*,Auto" ColumnDefinitions="Auto,*">

                                    <Image Grid.RowSpan="2"
                                       Source="derek.jpg"
                                       WidthRequest="60"
                                       HeightRequest="60"
                                       Aspect="AspectFill"
                                       VerticalOptions="Center"
                                       HorizontalOptions="Start"
                                       Margin="0,0,10,0">
                                        <Image.Clip>
                                            <EllipseGeometry Center="30,30" RadiusX="30" RadiusY="30"/>
                                        </Image.Clip>
                                    </Image>

                                    <Label Grid.Column="1"
                                       Text="{Binding name}"
                                       FontSize="15"
                                       VerticalOptions="Start"
                                       TextColor="Black"
                                       FontAttributes="Bold"
                                       LineBreakMode="WordWrap"
                                       Margin="0,0,0,5"/>

                                    <StackLayout Grid.Row="2" Grid.Column="0"
                                     Orientation="Vertical"
                                     VerticalOptions="End"
                                     HorizontalOptions="Start"
                                     Spacing="4">

                                        <Label Text="{Binding address}"
                   FontSize="14"
                   TextColor="Gray"
                   VerticalOptions="Center" />

                                        <Label Text="{Binding plus_code}"
FontSize="14"
TextColor="Gray"
VerticalOptions="Center" />

                                        <StackLayout Orientation="Horizontal" Spacing="5">
                                            <Label Text="{Binding rating, Converter={StaticResource RatingToStars}}"
                       FontSize="14"
                       TextColor="Goldenrod"
                       VerticalOptions="Center" />
                                            <Label Text="{Binding rating, StringFormat='{0:F1}'}"
                       FontSize="14"
                       TextColor="Black"
                       VerticalOptions="Center"/>
                                        </StackLayout>
                                    </StackLayout>

                                    <VerticalStackLayout Grid.Row="2" Grid.Column="1" HorizontalOptions="End" Spacing="10" Margin="0,15,0,0" WidthRequest="100">
                                        <Button Text="Location" Command="{Binding Source={RelativeSource AncestorType={x:Type local:repairShop}}, 
                                                  Path=OpenMapsCommand}"
                                    CommandParameter="{Binding maps_link}"
                    BackgroundColor="#21499F" TextColor="White"
                    CornerRadius="20" HeightRequest="35" WidthRequest="100" Padding="0"/>
                                        <Button Text="Message" Clicked="OnMessageClicked"
                    BackgroundColor="#34A853" TextColor="White"
                    CornerRadius="20" HeightRequest="35" WidthRequest="100" Padding="0"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>



            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>
